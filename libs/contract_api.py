# cryptoCache functions used to communicate with contracts

# Import necessary libraries
import requests
import json
from dotenv import load_dotenv
import os

def pull_order_information(token_id):
    return token_id
